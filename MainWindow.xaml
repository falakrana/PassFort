<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PasswordManager.ViewModels"
        xmlns:views="clr-namespace:PasswordManager.Views"
        xmlns:helpers="clr-namespace:PasswordManager.Helpers"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="680" Width="980"
        WindowStartupLocation="CenterScreen"
        Background="#060D06"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:InverseBooleanToVisibilityConverter x:Key="InvBoolToVis"/>
    </Window.Resources>

    <!-- Global Keyboard Accessibility Shortcuts -->
    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding AddNewCommand}" />
        <KeyBinding Key="F" Modifiers="Control" Command="{Binding ClearSearchCommand}" />
        <KeyBinding Key="L" Modifiers="Control" Command="{Binding LockCommand}" />
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}" />
    </Window.InputBindings>

    <Grid Background="#060D06">
        <!-- LOGIN / SETUP VIEW (Visible when Vault is Locked) -->
        <views:LoginView DataContext="{Binding LoginViewModel}"
                         Visibility="{Binding DataContext.IsVaultUnlocked, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InvBoolToVis}}" />

        <!-- MAIN VAULT VIEW (Visible when Vault is Unlocked) -->
        <views:MainView Visibility="{Binding IsVaultUnlocked, Converter={StaticResource BoolToVis}}" />
    </Grid>
</Window>
